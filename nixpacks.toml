# Tell Nixpacks the app root is /app (repo root), not /app/server
# This prevents Nixpacks from snapshotting server/ as the primary package

[variables]
NIXPACKS_BUILD_CMD = "npm ci && npm run build && cd server && npm ci"
NIXPACKS_START_CMD = "cd server && node_modules/.bin/tsx index.ts"
