{
  "name": "propos",
  "version": "1.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "server": "cd server && npm run dev",
    "build": "vite build && node scripts/patch-html.cjs && node -e \"const{cpSync,rmSync,existsSync}=require('fs');if(existsSync('server/public'))rmSync('server/public',{recursive:true});cpSync('dist','server/public',{recursive:true});console.log('[sync] dist → server/public')\"",
    "preview": "vite preview",
    "start": "cd server && node_modules/.bin/tsx index.ts"
  },
  "dependencies": {
    "framer-motion": "^11.0.0",
    "react": "^18.3.0",
    "react-dom": "^18.3.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.0",
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
